# lib-integrate.properties — managed by /lib-integrate (see settings.gradle.kts managed block).
#
# kmp-product-flavors: pinned to Maven Central resolution.
# `.local=false` forces Maven Central even when the source tree IS present locally,
# so the build no longer depends on a sibling checkout existing at `.path`.
kmp-product-flavors.local=false
kmp-product-flavors.path=../../../../mbs/kmp-product-flavors/source/kmp-product-flavors/build-logic
